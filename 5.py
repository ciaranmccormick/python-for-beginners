def question5():
    """ Splicing is like indexing but instead of getting 1 item returned to
    get a list of items.
    e.g. my_list = [1, 2, 3, 4] then; my_list[1:3] => [2, 3]
    Splice the list l to get ["d", "e", "f"]
    """

    l = ["a", "b", "c", "d", "e", "f", "g"]
    a = l
    return a
assert question5 == ["d", "e", "f"]
